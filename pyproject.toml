[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "gamelib"
version = "0.1.0"
description = "game audio/graphics core"
requires-python = ">=3.10"

dependencies=[
    "pygame"
]